# 统一行尾。仓库在 Windows 开发、Linux 容器与 CI 上运行，
# 若 scripts/*.sh 被检出为 CRLF，bash 会报 `\r: command not found`。
* text=auto eol=lf

# 必须是 LF 才能执行
*.sh text eol=lf
Makefile text eol=lf

# 不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
