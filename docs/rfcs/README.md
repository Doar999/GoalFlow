# RFC：改变已确认决策的提案

## 什么时候需要 RFC

设计文档里的结论分三种状态：**已确认**、**建议 / 推荐**、**待定 / 未决**。RFC 用于两类情况：

1. **要偏离一个"已确认"的决策。**
2. **要把一个影响公共契约、数据模型或跨模块行为的"建议 / 待定"收敛成"已确认"。**

只影响单个工作包内部实现的选择**不需要** RFC，记录在该工作包交接卡的"决策与假设"一节即可。判断标准：**别人的代码或页面会不会因为这个选择而改变行为？** 会，就走 RFC。

## 流程

1. 复制 [0000-template.md](0000-template.md) 为 `NNNN-<slug>.md`，编号取当前最大值加一。
2. 提 PR，PR 里只包含这一个 RFC 文件，便于集中讨论。
3. 由对应模块负责人批准；影响产品行为的还需产品决策人确认。
4. 合并后**必须回写**：更新对应的 `docs/product/` 或 `docs/development/` 文档，把状态从"建议 / 待定"改为"已确认"。

第 4 步最容易被漏掉。漏掉它，下一个人的 AI 读到的还是"建议"，还会再猜一次。

## 与设计文档的分工

| 文档 | 承载什么 |
| --- | --- |
| `docs/development/` `docs/product/` | **最终结论**，自包含，不写演进过程 |
| `docs/rfcs/` | **为什么是这个结论**，包含考虑过的替代方案和放弃的理由 |

设计文档正文不要写"原方案是……后来改为……"。那属于 RFC 和 Git 历史。

## 首批可能需要 RFC 的事项

`06-delivery-plan.md`"当前需先收敛的事项"列出的内容，进入实现前多数需要走这条路径：

- OpenAI / Anthropic 具体 API 子集与模型能力门槛（provider 选型本身已由 [RFC 0001](0001-adopt-langchain-langgraph.md) 确认）
- 具体约束阈值的后续调整（领域规则、附件范围与验证方式已确认，见产品文档 10 与 11；可移动日期范围与投入阈值已确认，见产品文档 13）
- 启用语义检索或全文检索作为产品能力（向量方案已由 [RFC 0003](0003-sqlite-as-primary-store.md) 定为 sqlite-vec，但首版不启用）

已确认的默认值不需要 RFC，清单见 [05-module-contracts.md](../development/05-module-contracts.md)"默认值状态"。

## 索引

| 编号 | 标题 | 状态 |
| --- | --- | --- |
| 0000 | 模板 | — |
| [0001](0001-adopt-langchain-langgraph.md) | 首版 Agent 编排与模型接入改用 LangChain + LangGraph | 已接受 |
| [0002](0002-contract-pr-gate.md) | 修正契约 PR 门禁与 OpenAPI 生成流程的冲突 | 已接受 |
| [0003](0003-sqlite-as-primary-store.md) | 首版业务数据库由 seekdb 改为 SQLite，向量能力由 sqlite-vec 承载 | 草案 |
