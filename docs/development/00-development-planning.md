# 开发规划输入与文档计划

状态：前后端总体技术方案已确认，具体实现细节待设计，尚未开始实现。

## 输入约束

- 首版已确认同时支持 OpenAI 和 Anthropic 两个 provider；通过 LangChain 统一 chat model 抽象调用，用户可自定义相应协议地址，详见模型接入设计。

- 最新确认：项目面向个人用户开源并强调开箱即用，默认允许自由注册登录，不设置邀请码；用户可以配置自己的模型和凭证。详见 [用户模型接入](07-model-provider-design.md)。

- 已确认首版 Agent 编排采用 LangGraph，模型接入采用 LangChain。图为短生命周期，不启用 checkpointer；等待用户与业务版本仍由 seekdb 承载。仅实现本产品所需流程，不采用框架的预制 agent 循环与记忆/检索组件；模型供应商由用户自行配置。

- 网页端，面向个人用户；同一实例支持多个隔离账号，主要在中国大陆使用。
- 已确认前端采用 React + TypeScript + Vite + shadcn/ui；后端采用 Python + FastAPI，数据库采用 seekdb，数据访问与迁移保留 SQLAlchemy + Alembic，后台任务采用 Celery + Redis。选型依据见前端选型比较及后端架构提案。
- 预算讨论暂缓；保留费用观测与限额设计，不决定具体支出。
- 多人使用 AI 协调开发；当前按多位开发参与者借助各自 AI 工具协作理解，具体分工待明确。
- 产品需求与已确认决策见 ../product/ 下文档；讨论建议不自动视为已确认要求。

## 协作设计建议

- 以统一 PRD、业务规则及验收条件作为各开发参与者和 AI 的共同输入。
- 开发任务标明范围、输入输出、依赖、验收条件及负责人；接口和数据结构先达成一致再并行实现。
- 任务使用独立分支和评审流程，公共接口与数据库变更指定负责人，避免同时修改造成不兼容。
- 共享测试场景覆盖时间预算冲突、目标隔离、任务依赖、计划启用、完成验证和用户数据隔离。
- 任务交付记录已完成内容、验证结果和未解决问题，便于交接。

## 后续文档

已汇总 [首版 PRD](../product/PRD.md)，并提供 [开发任务与验收](06-delivery-plan.md)。任务尚未开工，人员与日期未分配。

已补充可评审设计稿：

- [核心数据模型](03-data-model.md)：实体、版本、时间预算、后台作业及事务协议。
- [Agent 工作流](04-agent-workflows.md)：状态、结构化输出、确认、恢复及验收场景。
- [模块契约](05-module-contracts.md)：接口草案、错误规则与多人开发衔接。
- [前端工程架构](09-frontend-architecture.md)：路由、服务端状态、OpenAPI 类型、表单与 SSE 约定。

以上是逻辑设计，尚未执行 seekdb 兼容性验证、生成正式迁移或开始应用实现。

首版后端总体架构已确认并整理为 [后端架构提案](02-backend-proposal.md)，数据库按用户选择改为 seekdb；具体版本及兼容性仍需验证。

1. 整理首版 PRD，分开已确认、建议默认值与未决事项。
2. 定义核心对象、状态转换、模块责任和接口契约。
3. 验证 seekdb 数据访问及迁移兼容性，补齐前端路由与数据请求选型；选供应商前验证其当前服务条件及大陆实际可用性，不预设结论。
4. 形成数据库、API、后台任务、模型调用与权限设计。
5. 按依赖拆分开发任务，定义集成验收与首轮试用部署步骤。

## 待明确

- seekdb 版本、驱动及迁移兼容性，前端路由与数据请求方案，以及代码托管方式和 AI 工具分工。
- 首轮试用时间目标。
- 模型和部署选型；预算按用户要求稍后讨论。
