# 首版开发任务与验收 v0.1

状态：任务拆解建议，全部未开始，未分配实际人员或创建外部任务。顺序表示依赖，不是日历承诺。需求编号来自 [PRD](../product/PRD.md)。

## 1. 已确认技术基线

前端 React + TypeScript + Vite + shadcn/ui；Python + FastAPI；seekdb；SQLAlchemy + Alembic；Celery + Redis + Beat；SSE；Docker Compose + Nginx。Agent 自研工作流，通过供应商官方 SDK 加薄封装调用。具体版本和供应商未固定。

## 2. 工作包

T08、T14 的协议范围已明确为 OpenAI/Anthropic 双官方 Python SDK；两套原生路径分别验收，不以单一兼容协议通过代替。API 模式子集、版本及模型能力门槛仍在具体契约中细化。

| ID | 工作包与建议负责人角色 | 依赖/输入 | 交付与验收 | 对应需求 |
| --- | --- | --- | --- | --- |
| T01 | seekdb 兼容性验证：数据负责人 | 架构和数据模型；目标版本在验证通过后写死 | 可重复运行的事务、唯一约束、JSON、并发、反射、迁移与恢复验证；记录不支持项和采用的替代实现。用例清单见 [T01 交接卡](../worklog/T01-seekdb-verification.md) | Q02、Q03 |
| T02 | 工程与契约基础：集成负责人 | 已选栈；接口草案；前端工程架构 | 目录、锁文件、构建检查、错误结构、请求去重与 revision 契约；FastAPI OpenAPI 生成前端类型并检查漂移 | 全部基础 |
| T03 | 注册与账号会话：后端 | T01、T02、D07 | 自由注册、密码凭证、登录/退出、会话撤销、部署者辅助重置及可配置注册开关；双用户越权、并发同名注册和敏感值脱敏通过 | R01、Q01 |
| T04 | 目标与计划版本：后端 | T01、T02；D12 已确认 | 目标、来源化档案草稿、确认档案、路线、阶段、里程碑、任务批次与计划草稿；战略版本不可变、草稿不启用、生命周期命令 | R02、R04、R05、R10 |
| T05 | 时间预算与多目标排期：业务规则负责人 | T02；D03、D05、D06 | 确定性分层排期、目标 focus/rank、用户锁定、周/日容量、拆分、原因码和冲突输出；总额/剩余额度不混用，预算为零和并发变更有覆盖 | R06—R08、Q04、Q08 |
| T06 | 目标关联与任务依赖：后端 | T04；D04、D05 已确认 | 关联确认、无环校验、隔离规则；解除关联先处理未完成依赖；跨用户关联被拒 | R07、Q01 |
| T07 | 作业执行与恢复：后台负责人 | T01、T02 | outbox、领取租约、有限重试、取消和持久化事件；重复投递/旧 Worker 晚返回不重复提交 | Q02、Q03 |
| T08 | 模型接入与领域策略：Agent 负责人 | T02、T07、T14；D01、D08、D09 | SDK 协议适配、目标澄清、内部领域路由、结构化输出、提示版本与用量；规则层计算就绪状态，用户无需分类，分别完成固定样例与真实模型评估 | R02、R03、R14、R15、Q07 |
| T09 | 规划到启用闭环：Agent 与后端 | T03—T08；D02、D03 | 档案确认、路线比较、滚动计划草稿、七日任务批次和原子启用；失效版本不启用，双目标争用额度不双重成功 | R02—R08、Q02、Q04 |
| T10 | 前端核心页面：前端 | T02 契约；联调依赖 T03、T09 | 路由、会话保护、登录、设置、目标创建、方案比较和计划预览；用户切换不残留缓存，可先基于明确标记的样例开发 | R01—R08 |
| T11 | 今日页与执行记录：前后端 | T04、T05、T07；D10 | 多目标清单、一键/部分完成、今日延期、持久阻碍、更正、材料和独立验证；任务身份不因重排变化 | R06、R10—R12、Q08 |
| T12 | 反馈调整与回顾：Agent 与规则负责人 | T06、T08、T11；D06、D11 已确认 | 按计划包络判定 A0—A4，自动调整留原因、重大变化待确认；重复摩擦诊断、拒绝/过期处理和可跳过回顾 | R09、R13、Q05 |
| T13 | 试用版集成与运维：集成负责人 | T09—T12；托管和数据保留选择 | 部署说明、备份恢复演练、大陆网络实测、用量及错误观测，完整验收记录 | Q01—Q08 |
| T14 | 个人模型配置：前后端与数据负责人 | T02、T03、T07；D08 | 配置增删改、加密、默认选择、测试和错误展示；跨用户访问、脱敏、撤销及自定义地址限制验收通过。最小测试调用直接接协议适配，不依赖领域策略 | R15、Q01、Q03、Q07 |
| T15 | 开源自部署交付：集成负责人 | T13、T14；许可证已定为 Apache-2.0 | 无凭证配置样例、启动/升级/备份说明、兼容矩阵；干净环境验证部署。正式发布时间与推广方式另行确认 | 开源定位、Q06 |

T09 的 T03—T08 指账号、计划、排期、关联、作业、模型能力集成就绪；可先用固定模型样例联调，但不能据此宣称真实 Agent 质量通过。

## 3. 里程碑

| 里程碑 | 完成条件 | 允许开展的下一阶段 |
| --- | --- | --- |
| M0 技术基础可验证 | T01、T02；明确数据库不兼容项，公共契约稳定 | 账号、业务规则、后台与页面并行 |
| M1 业务链路可运行 | 账号、草稿、预算、去重、执行记录可用；模拟模型明确标识 | 接入真实模型并完善交互 |
| M2 真实规划闭环 | 用户可从表达目标到启用计划再提交第一条反馈 | 多目标与连续使用验证 |
| M3 多目标与调整完整 | 隔离、关联、冲突、确认、记录更正和回顾通过 | 首轮试用准备 |
| M4 可发布试用版 | 实际环境部署、注册登录、恢复、数据隔离及模型链路验证完成 | 个人用户试用与开源发布准备 |

M1 是内部开发检查点，不能以单目标样例通过替代首版多目标交付。

## 4. 每项任务交付要求

负责人领取时记录允许修改的模块、依赖版本、输入/输出、验收场景、未决选择。交付包括代码/迁移、接口样例、验证结果与已知限制。公共迁移、共享类型及配置由指定负责人协调。

测试集中在真实规则：数据库回滚、版本竞争、预算、依赖、用户隔离和模型异常。文案或低影响样式修改不需要重复建设镜像测试。使用真实 seekdb 进行数据库验收，不能用 SQLite 通过结果代替。

自研工作流单独验收等待用户不占 Worker、重启可恢复、恢复后重新检查版本、原子确认和取消竞争。LangChain/LangGraph 不属于本轮实现依赖。

## 5. 首个集成演示脚本

1. 新用户无需邀请码完成注册，配置模型和默认时间，创建学习目标，查看并修正档案。
2. 比较候选路线，微调后查看首周安排；未启用前今日页不出现草稿任务。
3. 启用后重复点击，确认任务不重复；提交一条部分完成记录。
4. 新建独立目标并制造预算冲突，确认系统不自动降低原目标要求。
5. 明确关联两个目标，添加合法依赖；反向依赖产生环时被拒。
6. 缩减今天剩余时间，查看小调整原因；重大延期需确认，拒绝后原期限保留。
7. 模型作业运行时刷新页面或重启 Worker，恢复后只发布一个有效业务结果。
8. 第二个用户尝试访问第一个用户的任务、附件与作业事件，均被拒。

## 6. 当前需先收敛的事项

可独立推进设计：seekdb 验证用例、接口 schema、页面信息结构和排期规则测试样例。

开始对应实现前需收敛：OpenAI/Anthropic 具体 API 子集与能力门槛。D01 三类领域范围及其专业规则、D07 自由注册登录、D08 双 SDK 及 D09/D10 辅助能力与附件范围已确认，其他默认值按 PRD 决策表集中确认。附件、链接与验证的实现基线见 [辅助能力、材料解析与验证实现基线](15-assistance-and-verification-design.md)，领域硬约束与校验点见 [领域策略包与约束校验实现基线](16-domain-policy-design.md)；其中新增接口与 `artifacts` 字段仍需契约 PR。预算金额继续暂缓，不在本计划指定付费资源。
